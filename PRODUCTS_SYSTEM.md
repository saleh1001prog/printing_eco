# نظام إدارة المنتجات الديناميكي

تم تحويل الموقع إلى نظام ديناميكي كامل لإدارة المنتجات!

## ✨ المميزات الجديدة

### 1. **نموذج Product في MongoDB**
- تخزين جميع معلومات المنتجات في قاعدة البيانات
- دعم المميزات، الفوائد، لقطات الشاشة، التقنيات، والمتطلبات
- حالات النشر (مسودة/منشور)
- ترتيب المنتجات
- منتجات مميزة

### 2. **API Endpoints**
```
GET    /api/products              # جلب جميع المنتجات
POST   /api/products              # إضافة منتج جديد (Admin)
GET    /api/products/[slug]       # جلب منتج واحد
PATCH  /api/products/[slug]       # تحديث منتج (Admin)
DELETE /api/products/[slug]       # حذف منتج (Admin)
POST   /api/upload                # رفع صورة إلى Cloudinary (Admin)
DELETE /api/upload                # حذف صورة من Cloudinary (Admin)
```

### 3. **لوحة التحكم الإدارية**
- **الرابط**: `/admin/products`
- عرض جميع المنتجات مع الصور
- إضافة منتج جديد
- تعديل منتج موجود
- حذف منتج
- رفع الصور إلى Cloudinary
- معاينة المنتج

### 4. **صفحات المنتجات الديناميكية**
- **الرابط**: `/products/[slug]`
- تعرض جميع معلومات المنتج من قاعدة البيانات
- تصميم احترافي ومتجاوب
- دعم كامل للغة العربية
- SEO محسّن

## 🚀 كيفية الاستخدام

### 1. التثبيت
```bash
npm install cloudinary
```

### 2. إعداد متغيرات البيئة
تأكد من وجود هذه المتغيرات في `.env.local`:
```env
# MongoDB
MONGODB_URI=mongodb+srv://...

# Admin
ADMIN_API_KEY=your-api-key

# Cloudinary
CLOUDINARY_CLOUD_NAME=dpuynckww
CLOUDINARY_API_KEY=231676298228287
CLOUDINARY_API_SECRET=BkTXidIk8H-awWZxDuh8swON_3k

# Optional (للإنتاج)
NEXT_PUBLIC_BASE_URL=https://yourdomain.com
```

### 3. الوصول إلى لوحة التحكم
1. اذهب إلى `/admin`
2. أدخل مفتاح API الإداري
3. اضغط على "إدارة المنتجات"
4. ابدأ بإضافة منتجاتك!

## 📝 إضافة منتج جديد

1. اذهب إلى `/admin/products`
2. اضغط على "إضافة منتج جديد"
3. املأ المعلومات الأساسية:
   - الاسم (عربي/إنجليزي)
   - Slug (رابط المنتج)
   - النوع (Desktop/Web/Mobile/Enterprise)
   - الوصف
   - Badge (اختياري)
4. ارفع لقطات الشاشة
5. أضف معلومات التحميل (إذا كان متاحاً)
6. اختر الحالة (مسودة/منشور)
7. احفظ!

## 🎨 تخصيص صفحة المنتج

كل منتج يمكن أن يحتوي على:
- **Hero Section**: عنوان، وصف، badge، أزرار
- **Benefits**: فوائد المنتج (حتى 4)
- **Features**: مميزات رئيسية (غير محدودة)
- **Screenshots**: لقطات شاشة مع أوصاف
- **Technologies**: التقنيات المستخدمة
- **Requirements**: متطلبات النظام
- **Download Section**: معلومات التحميل

## 🔗 الروابط المهمة

- **الصفحة الرئيسية**: `/` (ديناميكية - تعرض المنتجات من قاعدة البيانات)
- **جميع المنتجات**: `/products` (ديناميكية - مصنفة حسب النوع)
- **صفحة منتج**: `/products/[slug]` (ديناميكية)
- **لوحة التحكم**: `/admin`
- **إدارة المنتجات**: `/admin/products`
- **إدارة التفعيلات**: `/admin` (الصفحة الرئيسية للوحة التحكم)

## ✅ تم إنجازه

1. ✓ **نموذج Product في MongoDB** - كامل مع جميع الحقول
2. ✓ **API Endpoints** - جلب، إضافة، تعديل، حذف المنتجات
3. ✓ **رفع الصور إلى Cloudinary** - API كامل
4. ✓ **لوحة تحكم احترافية** - عرض وإدارة المنتجات
5. ✓ **نموذج إضافة/تعديل** - شامل مع رفع الصور
6. ✓ **صفحة منتج ديناميكية** - `/products/[slug]`
7. ✓ **الصفحة الرئيسية ديناميكية** - تعرض المنتجات من قاعدة البيانات
8. ✓ **صفحة جميع المنتجات** - `/products` مع تصنيف حسب النوع

## 📦 ميزات إضافية (اختياري للمستقبل)

1. **محرر نصوص غني** (Rich Text Editor) للأوصاف والمميزات
2. **نظام بحث متقدم** للمنتجات
3. **فلاتر ديناميكية** (حسب النوع، السعر، التقييم، إلخ)
4. **نظام تقييمات** للمنتجات
5. **صفحة مقارنة** بين المنتجات
6. **نظام إشعارات** للمنتجات الجديدة

## 🎯 الملفات المهمة

```
inventorywebapp/
├── models/
│   └── Product.ts                    # نموذج المنتج
├── app/
│   ├── page.tsx                      # الصفحة الرئيسية (ديناميكية)
│   ├── api/
│   │   ├── products/
│   │   │   ├── route.ts              # API للمنتجات
│   │   │   └── [slug]/route.ts       # API لمنتج واحد
│   │   └── upload/route.ts           # API للصور
│   ├── admin/
│   │   ├── page.tsx                  # لوحة التحكم (التفعيلات)
│   │   └── products/
│   │       ├── page.tsx              # صفحة إدارة المنتجات
│   │       └── ProductForm.tsx       # نموذج إضافة/تعديل
│   └── products/
│       ├── page.tsx                  # صفحة جميع المنتجات
│       ├── [slug]/page.tsx           # صفحة المنتج الديناميكية
│       └── inventory-pro/page.tsx    # (قديم - يمكن حذفه)
```

## 💡 نصائح

- استخدم slugs واضحة ومفهومة (مثل: `inventory-pro`, `hr-system`)
- ارفع صور عالية الجودة للقطات الشاشة
- اكتب أوصاف واضحة ومفيدة
- استخدم الحالة "مسودة" للمنتجات قيد التطوير
- رتب المنتجات باستخدام حقل "order"

---

**تم بنجاح! 🎉**

الآن يمكنك إدارة جميع منتجاتك من لوحة التحكم بدون الحاجة لتعديل الكود!
